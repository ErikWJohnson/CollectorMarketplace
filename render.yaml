services:
  - type: web
    name: collector-marketplace
    runtime: node
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /healthz
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: collector-db
          property: connectionString
